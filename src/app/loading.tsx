import LoadingSpinner from "@/components/ChatProject/LoadingSpinner";

export default function Loading() {
  return (
    <div className="flex items-center justify-center p-10">
      <LoadingSpinner />
    </div>
  );
}
